# Geo-Location Estimation
Estimate the geo-location of a point visible in an image, given that we have some required meta-data.

## Setup
Install the requirements either using pip
```shell
pip install -r requirements.txt
```
or using conda
```shell
conda install --file requirements.txt
```

